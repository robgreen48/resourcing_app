<%= form_for(@planned_hour) do |f| %>
  <% if @planned_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@planned_hour.errors.count, "error") %> prohibited this planned_hour from being saved:</h2>

      <ul>
      <% @planned_hour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :client_id %><br>
    <%= f.number_field :client_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :month %><br>
    <%= f.datetime_select :month %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
